// vim:ts=4:sw=4
var emulator = exports,
    lifter = require('../lifter');

//
// ### function execute (map, iteration)
// #### @map {Map} Instance of Map object
// #### @iteration {Number} Number of emulator's iteration
// Move all rocks on maps to new position and return new map instance
//
emulator.execute = function execute(map, iteration) {
};
